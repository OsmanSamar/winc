const add = (a, b) => a + b;
const substract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const speedOfLightMetersPerSecond = 299792458;
//export { add, substract, multiply, divide, speedOfLightMetersPerSecond};
module.exports = {
  add,
  substract,
  multiply,
  divide,
  speedOfLightMetersPerSecond,
};
